use "testing"
use "table"

testing.test("table.render basic", lam{ in
    let rows = [{a: 1, b: 2}, {a: 3, b: 4}]
    let out = table.render(rows)
    testing.expect(out.contains("+"), "should have border")
    testing.expect(out.contains("1"), "should contain data")
    testing.expect(out.contains("4"), "should contain data")
})

testing.test("table.render with columns and headers", lam{ in
    let rows = [{x: 10, y: 20}]
    let out = table.render(rows, {
        columns: ["x", "y"],
        headers: {x: "X", y: "Y"}
    })
    testing.expect(out.contains("X"), "should have custom header")
    testing.expect(out.contains("Y"), "should have custom header")
})

testing.test("table.render borderless", lam{ in
    let rows = [{a: 1}]
    let out = table.render(rows, {border: false})
    testing.expect(!out.contains("+"), "should not have border")
    testing.expect(out.contains("1"), "should contain data")
})

testing.test("table.render right align", lam{ in
    let rows = [{n: 1}, {n: 100}]
    let out = table.render(rows, {columns: ["n"], align: {n: "right"}})
    testing.expect(out.contains("  1"), "should right-align")
})

testing.test("table.render empty rows", lam{ in
    testing.expectEqual(table.render([]), "", nil)
})

testing.test("table.kv", lam{ in
    let out = table.kv({a: 1, b: 2})
    testing.expect(out.contains("a"), "should contain key")
    testing.expect(out.contains("1"), "should contain value")
})

testing.done()
