use "testing"

class Animal {
    func init(name) {
        this.name = name
    }
    func speak() {
        return "%{this.name} makes a noise"
    }
}

class Dog extends Animal {
    func init(name, breed) {
        super.init(name)
        this.breed = breed
    }
    func speak() {
        return "%{this.name} barks"
    }
    func describe() {
        return super.speak() + " (it's a " + this.breed + ")"
    }
}

testing.test("instance fields and methods", lam{ in
    let a = Animal("Leo")
    testing.expectEqual(a.name, "Leo", nil)
    testing.expectEqual(a.speak(), "Leo makes a noise", nil)
})

testing.test("inheritance and override", lam{ in
    let d = Dog("Rex", "husky")
    testing.expectEqual(d.name, "Rex", nil)
    testing.expectEqual(d.breed, "husky", nil)
    testing.expectEqual(d.speak(), "Rex barks", nil)
})

testing.test("super", lam{ in
    let d = Dog("Rex", "husky")
    testing.expectEqual(d.describe(), "Rex makes a noise (it's a husky)", nil)
})

testing.test("field assignment", lam{ in
    let a = Animal("Cat")
    a.name = "Whiskers"
    testing.expectEqual(a.name, "Whiskers", nil)
})

testing.test("isInstance-like type check", lam{ in
    let a = Animal("X")
    testing.expectEqual(type(a), "instance", nil)
})

testing.done()
